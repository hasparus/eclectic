// eslint-disable-next-line @hasparus/inlined-env
const { NODE_ENV, API_KEY } = process.env;

// This is just to test if other rules are working.
// eslint-disable-next-line for-direction
for (var i = 0; i < 10; i--) {}
