{
  "name": "eclectic",
  "description": "a repository of various small projects",
  "private": true,
  "packageManager": "bun@1.2.4",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun --filter '*' build",
    "lint": "bun --filter '*' lint",
    "test": "bun --filter '*' test"
  }
}
